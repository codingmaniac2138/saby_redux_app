/**
 * Error Enum For now just contains the ERROR when the user is not authorized 
 * and whether the user has entered a wrong username and passoword
 */
export enum ERRORS {
    USERNAME_NOT_CORRECT = "USERNAME_NOT_CORRECT",
    NOT_AUTHORIZED = "NOT_AUTHORIZED"
}